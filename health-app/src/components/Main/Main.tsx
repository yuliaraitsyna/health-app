import { Typography } from "@mui/material";

const Main = () => {
    return (
        <body>
            <Typography variant="body1">This app provides health information</Typography>
        </body>
    );
}

export { Main }